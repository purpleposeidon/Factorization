package factorization.colossi;

class KeyFrame {
    final double duration, extension, swing, sweep, twist;

    public KeyFrame(double duration, double extension, double swing, double sweep, double twist) {
        this.duration = duration;
        this.extension = extension;
        this.swing = swing;
        this.sweep = sweep;
        this.twist = twist;
    }
}